# Weksa Scratch

Disposable working surface. Promote only durable decisions into
`state/map.yaml` or `schemas/`.

## Current Thread

Weksa is named after a PIE-ish speech root flavor. The core idea is a
procedural alien-language engine that avoids English word substitution by
rendering structured meaning through an alien ontology and language pipeline.

## Immediate Next Questions

- Decide the first artifact format for a language project.
- Decide whether the first prototype should be notebook-like, CLI-first,
  library-first, or editor/tool-first.
- Evaluate implementation language after the data model is clearer.
