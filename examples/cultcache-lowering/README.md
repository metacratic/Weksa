# CultCache Lowering Examples

These examples lower interlingua packets through CultCache cultural ontology
profiles rather than the older YAML-only target-language profiles.

The starter database is:

- `.weksa/cultural-ontology/starter-targets.cc`

Each target file is produced as a separate agent run so differences in target
ontology remain inspectable instead of being smoothed into one multilingual
batch.
